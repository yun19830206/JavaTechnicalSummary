<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cloud.aiassistant.formdata.dao.TableDataAuthMapper">

    <sql id="Base_Column_List">
        id, from_user, from_table, to_user, data_auth_type, create_user, create_time, tenant_id
    </sql>
    <select id="selectByPrimaryKey" resultType="com.cloud.aiassistant.pojo.formdata.TableDataAuth">
        select
          <include refid="Base_Column_List"/>
        from pub_auth_form_data
        where id = #{id,jdbcType=INTEGER}
    </select>

    <select id="selectByUserIdAndTableId" resultType="com.cloud.aiassistant.pojo.formdata.TableDataAuth">
        select
            a.from_user, a.from_table, a.to_user, a.data_auth_type, a.create_user, a.create_time, a.tenant_id, b.user_name 'to_user_name'
        from pub_auth_form_data a , pub_user b
        where a.to_user = b.id
            and from_user = #{fromUser} and from_table = #{fromTable}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        DELETE FROM pub_auth_form_data
        WHERE id = #{id}
    </delete>

    <delete id="deleteByUserIdAndTableId">
        DELETE FROM pub_auth_form_data
        WHERE from_user = #{fromUser} and from_table = #{fromTable}
    </delete>

    <insert id="insert">
        INSERT INTO pub_auth_form_data (from_user,
                                        from_table,
                                        to_user,
                                        data_auth_type,
                                        create_user,
                                        tenant_id)
        VALUES (#{fromUser},
                #{fromTable},
                #{toUser},
                #{dataAuthType},
                #{createUser},
                #{tenantId})
    </insert>

    <insert id="insertBatch">
        INSERT INTO pub_auth_form_data (from_user,
                                        from_table,
                                        to_user,
                                        data_auth_type,
                                        create_user,
                                        tenant_id)
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.fromUser},
            #{item.fromTable},
            #{item.toUser},
            #{item.dataAuthType},
            #{item.createUser},
            #{item.tenantId})
        </foreach>
    </insert>
</mapper>