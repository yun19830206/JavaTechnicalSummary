## Ajax接口通用说明
  如无特殊说明，Ajax的method方式统一为post,Content-Type: application/json

## 1：微信身份认证页面 绑定接口
```javascript
    url: http://{host}/aiassistant/user/wx/binding

    parameter: JSON
        {
            userName:"userName",    //[必填]身份认证页面用户名输入框
            password:"password",    //[必填]身份认证页面密码输入框
            wxCode:"wxCode"         //[必填]身份认证页面
        }
    
    result: JSON
        {
            "code": 200,            //绑定结果状态码。 200成功，500失败
            "message": "绑定成功，请返回让小助手热情为你服务吧。",   //绑定成功与失败描述
            "debugMessage": null,
            "data": null
        }
```

## 2：机器人调用微信身份认证接口
```javascript
    url: http://{host}/aiassistant/user/wx/getwxuser?openid=good       //good为示例，实则是真正的微信openid
    
    parameter: JSON
        {}
    
    result: JSON
        {
            "code": 200,            //调用结果状态码。 200成功，500失败
            "message": "success",   //调用成功与失败描述
            "debugMessage": null,
            "data": {               //500失败时此值为null，200成功时为如下JSON
                "id": 1,                    //用户唯一标识
                "userName": "程云",         //姓名
                "password": "****",         //密码，经过处理了 
                "createTime": "2019-03-10T04:11:47.000+0000",  //创建时间
                "createUser": null,         //创人ID
                "tenantId": 200,            //租户标识
                "wxOpenid": "good"          //微信OpenId
            }            
        }
```

## 3：登入接口
```javascript
    url: http://{host}/aiassistant/user/login/userlogin       //good为示例，实则是真正的微信openid
    
    parameter: JSON
        {
            userName:"userName",    //[必填]登入页面用户名输入框
            password:"password"     //[必填]登入页面密码输入框
        }
    
    result: JSON
        {
            "code": 200,            //调用结果状态码。 200成功，500失败
            "message": "success",   //调用成功与失败描述
            "debugMessage": null,
            "data": {               //500失败时此值为null，200成功时为如下JSON
                "id": 1,                    //用户唯一标识
                "userName": "程云",         //姓名
                "password": "****",         //密码，经过处理了 
                "createTime": "2019-03-10T04:11:47.000+0000",  //创建时间
                "createUser": null,         //创人ID
                "tenantId": 200,            //租户标识
                "wxOpenid": "good"          //微信OpenId
            }            
        }
```

## 4-1：创建一个表单设计器表的(表单配置、字段配置)接口
```javascript
    url: http://{host}/aiassistant/formdesign/create/formdesigntable
    
    parameter: JSON
        {}
    
    result: JSON
        {
            "code": 200,            //调用结果状态码。 200成功，500失败
            "message": "success",   //调用成功与失败描述
            "debugMessage": null,
            "data": {               //500失败时此值为null，200成功时为JSON
                "id": 1,                    //用户唯一标识
            }            
        }
```

## 4-2：创建一个表单设计器表的(查询条件配置、展示结果配置)接口
```javascript
    url: http://{host}/aiassistant/formdesign/create/formdesignquery
    
    parameter: JSON
        {}
    
    result: JSON
        {
            "code": 200,            //调用结果状态码。 200成功，500失败
            "message": "success",   //调用成功与失败描述
            "debugMessage": null,
            "data": {               //500失败时此值为null，200成功时为JSON

            }            
        }
```

## 4：修改一个表单的查询条件配置 与 展示结果配置 接口
```javascript

    
```

## 5：获得一个表单的配置结果 接口
```javascript

    
```

## 6：增加一个表单的创建数据 接口
```javascript

    
```

## 7：一个表单查询数据结果(我创建的  和  赋权给我的)
```javascript

    
```

## 8：获得我的配置表单数据(我创建的 和 赋权给我的)
```javascript

    
```